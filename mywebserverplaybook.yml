---
- hosts : all
  become : yes
  tasks:
  - name: i want to install webserver in ubuntu node
    apt:
      name : apache2
      state : present

  - name : i want to run the server
    service:
      name : apache2
      state : stopped
